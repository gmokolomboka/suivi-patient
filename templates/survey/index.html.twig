{% extends 'base.html.twig' %}

{% block title %}Questionnaire aidant {% endblock %}

{% block body %}
   <h3 class="text-center"> Votre questionnaire aidant a été enregistré avec succès</h3>
   <div class="row">
       <div class="col-8 offset-2">
       <div class="card">
           <div class="card-footer text-muted small">
               {{ survey.created|date("m/d/Y h:i a") }}
           </div>
       </div>
       </div>
   </div>

{% endblock %}
